apiVersion: apps/v1
kind: Deployment
metadata:
#  name: address-service-k8s-deployment
  name: {{ .Release.Name }}-address-service-k8s-deployment
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
#      app: address-service
      app: {{ .Release.Name }}-address-service
  template:
    metadata:
      labels:
#        app: address-service # This selector name should match with the mysql-service selector-app section
        app: {{ .Release.Name }}-address-service # This selector name should match with the mysql-service selector-app section
    spec:
      containers:
#          - name: address-service-k8s-container
          - name: {{ .Release.Name }}-address-service-k8s-container
            image: {{ .Values.addressservice.image.repository }}:{{ .Values.addressservice.image.tag }}
            imagePullPolicy: {{ .Values.addressservice.image.pullPolicy }}
            ports:
              - containerPort: 9002
            env:
              - name: SPRING_DATASOURCE_URL
                value: {{ include "address-service.fullMySQLConnectionURL" . | quote }} # get the values from _helpers.tpl
              - name: SPRING_DATASOURCE_USERNAME
                valueFrom:
                  secretKeyRef:
#                    name: mysql-username
                    name: {{ .Release.Name }}-mysql-username
                    key: username
              - name: SPRING_DATASOURCE_PASSWORD
                valueFrom:
                  secretKeyRef:
#                    name: mysql-password
                    name: {{ .Release.Name }}-mysql-password
                    key: password
